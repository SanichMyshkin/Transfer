Скрипт выполняет комплексный аудит инфраструктуры Zabbix, собирая данные через Zabbix API и формируя детализированный Excel-отчёт.
В результате формируется полная инвентаризация пользователей, групп, ролей, хостов и связанных с ними объектов (триггеры, графики, дашборды).

Архитектура скрипта

Скрипт использует модульный подход с последовательным сбором данных через API Zabbix:

zabbix_utils — клиент для взаимодействия с Zabbix API
pandas — формирование и сохранение Excel-отчёта
dotenv — управление настройками через переменные окружения
1. Инициализация и настройки

1.1. Загрузка конфигурации

Скрипт использует .env файл для хранения чувствительных данных:

ZABBIX_URL — URL Zabbix-сервера
ZABBIX_TOKEN — токен аутентификации API
LOG_FILE — путь к файлу логов (по умолчанию: zabbix_userdata_report.log)
При отсутствии обязательных параметров скрипт завершается с ошибкой.

1.2. Настройка логирования

Настраивается двухканальное логирование:

Файловый обработчик — запись в указанный файл лога
Консольный обработчик — вывод в реальном времени
Формат: дата | уровень | сообщение с кодировкой UTF-8
2. Подключение к Zabbix API

2.1. Аутентификация

Используется токен-аутентификация через библиотеку zabbix_utils:

python
api = ZabbixAPI(url=ZABBIX_URL)
api.login(token=ZABBIX_TOKEN)
При успешном подключении фиксируется в логах, при ошибке — скрипт завершает работу.

3. Сбор данных из Zabbix

3.1. Пользователи (Users)

API-запрос: api.user.get() с расширенным набором полей:

Основные данные: userid, alias, username, name, surname, type
Настройки: autologin, autologout, lang, refresh, theme, timezone
Безопасность: attempt_ip, attempt_clock
Связанные объекты: группы, роли, сессии, медиа-контакты
Обработка данных:

Определение логина по приоритету: alias → username → "—"
Извлечение email из медиа-контактов (список sendto)
Форматирование групп пользователей и названий ролей
Конвертация временных меток в читаемый формат
Трансформация бинарных флагов ("1"/"0") в "Да"/"Нет"
Результат: список словарей с полной информацией о пользователях для листа "Пользователи".

3.2. Группы пользователей (Usergroups)

API-запрос: api.usergroup.get() с полями:

usrgrpid, name, gui_access, users_status
Список участников группы через selectUsers
Обработка данных:

Формирование строки с перечнем участников группы
Сохранение настроек доступа и статуса группы
Результат: структурированные данные для листа "Группы".

3.3. Роли (Roles)

API-запрос: api.role.get() с полями:

roleid, name, type, readonly
Обработка данных:

Конвертация флага readonly в "Да"/"Нет"
Сохранение типа и идентификатора роли
Результат: данные для листа "Роли".

3.4. Хосты и связанные объекты

3.4.1. Базовые данные хостов

API-запрос: api.host.get() с полями:

hostid, host, name, status
Интерфейсы, группы, шаблоны
3.4.2. Пакетная загрузка триггеров

Оптимизация: обработка хостов пачками по 500 элементов для избежания перегрузки API
API-запрос: api.trigger.get() с фильтрацией по hostids
Сбор статистики: подсчёт триггеров для каждого хоста через Counter

3.4.3. Пакетная загрузка графиков

Аналогично триггерам: api.graph.get() с пагинацией
Сбор статистики: подсчёт графиков для каждого хоста

3.4.4. Загрузка дашбордов

API-запрос: api.dashboard.get() — все дашборды системы
Сопоставление с хостами: поиск упоминаний имён хостов в названиях дашбордов

4. Формирование данных хостов

Структура записи:

Основная информация: ID, имя, системное имя, IP-адреса
Классификация: группы, шаблоны, статус активности
Статистика: количество триггеров, графиков, дашбордов
Статус: "Активен"/"Отключён" на основе поля status
Обработка:

Извлечение IP-адресов из интерфейсов
Форматирование списков групп и шаблонов
Конвертация статуса из числового в текстовый формат
Сопоставление статистических данных через объекты Counter
5. Сводная статистика

Формирование метрик:

Дата и время генерации отчёта
Количественные показатели: пользователи, группы, роли, хосты
Статистика активности: автологин, статусы хостов
Сводка по объектам: триггеры, графики, дашборды
6. Сохранение отчёта

6.1. Структура Excel-файла

📊 Сводка — итоговые метрики системы

👥 Пользователи — полный список пользователей с сортировкой по логину:

Идентификационные данные: ID, логин, имя
Контакты: email
Классификация: группы, роли
Активность: последний вход, IP, автовход
Настройки: язык, тема, обновление, часовой пояс
👪 Группы — группы пользователей с сортировкой по названию:

ID, название группы
Настройки доступа и статус
Список участников
🎭 Роли — системные роли с сортировкой по имени:

ID, название роли, тип
Флаг read-only режима
🖥️ Хосты — инвентаризация хостов с сортировкой по имени:

Сетевые параметры: имя, хост, IP
Классификация: группы, шаблоны
Статистика: триггеры, графики, дашборды
Статус активности
6.2. Технология сохранения

Используется pd.ExcelWriter с движком openpyxl для записи нескольких листов. Каждый DataFrame сохраняется на отдельный лист с сортировкой данных.

7. Завершение работы

Финальные операции:

Логирование успешного сохранения файла
Выход из API Zabbix (api.logout())
Фиксация завершения работы в логах
Итоговый результат: файл zabbix_full_report.xlsx с полной инвентаризацией Zabbix-инфраструктуры.

Итог:
Скрипт автоматизирует сбор и анализ данных Zabbix, предоставляя комплексный отчёт для аудита пользователей, хостов и связанных объектов. Оптимизированная пакетная загрузка данных обеспечивает стабильную работу с большими инфраструктурами, а детализированная структура отчёта позволяет проводить полный анализ системы мониторинга.