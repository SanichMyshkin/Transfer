нализ GitLab: сбор, обработка и формирование отчёта
Механизм работы:

Скрипт выполняет автоматизированный сбор данных из GitLab API, объединяя сведения о пользователях, проектах, раннерах и системной статистике в единый аналитический отчёт. Он используется для аудита состояния инстанса GitLab, анализа активности и оценки потребления хранилища.

Процесс работы делится на несколько этапов:

подключение к GitLab API;

извлечение данных по ключевым сущностям (users, projects, runners, statistics);

формирование итогового Excel-отчёта.

1. Подключение и принципы отбора данных
Подключение к GitLab API

Сценарий использует библиотеку python-gitlab, подключаясь к серверу GitLab по токену (GITLAB_TOKEN) и адресу (GITLAB_URL).
SSL-проверка отключена для совместимости с внутренними установками GitLab, и все сетевые предупреждения подавляются. После аутентификации выполняется базовая проверка соединения (gl.auth()), обеспечивающая доступ ко всем REST-эндпоинтам GitLab API.

2. Основные источники данных и логика формирования
2.1. Пользователи (GET /users)

Функция get_users() запрашивает список всех пользователей GitLab через gl.users.list(all=True, iterator=True).
Из каждой учётной записи извлекаются поля:

id, username, email, name;

last_sign_in_at — дата последнего входа;

last_activity_on — последняя активность;

extern_uid — внешние идентификаторы (например, LDAP или OIDC).

Если у пользователя есть несколько внешних идентификаторов, они объединяются в одну строку через запятую.
Все данные собираются в список, который затем формирует лист Пользователи в Excel-отчёте.

2.2. Общая статистика GitLab (GET /statistics)

Метод get_stat() обращается к эндпоинту /statistics, доступному администраторам GitLab, и извлекает основные показатели состояния системы:

количество форков, issues, merge requests, notes, snippets, milestones;

число пользователей, групп, проектов;

активные пользователи (active_users);

объёмы SSH-ключей и других сущностей.

Возвращаемые значения могут быть строками (например "12,345"), поэтому перед добавлением в отчёт выполняется очистка и преобразование к числу.
Результат попадает на лист Статистика.

2.3. Проекты (GET /projects)

Функция get_projects_stats() собирает информацию обо всех проектах GitLab, но ограничивает выборку значением MAX_PROJECTS (по умолчанию 200).
Для каждого проекта запрашиваются статистические данные (statistics=True):

repository_size, lfs_objects_size, job_artifacts_size, storage_size — объёмы хранилища (в байтах, пересчитываются в мегабайты);

commit_count — количество коммитов;

last_activity_at — время последней активности;

visibility — уровень доступа (private, internal, public).

Данные агрегируются в таблицу Проекты, где каждый проект занимает отдельную строку.
Дополнительно суммируется общее количество коммитов по всем проектам (total_commits) — эта метрика добавляется в Статистику и используется для оценки активности репозиториев.
Результаты сортируются по размеру хранилища, чтобы наглядно показать, какие проекты занимают больше всего места.

2.4. Раннеры (GET /runners/all)

Функция get_runners_info() запрашивает список всех CI/CD-раннеров (gl.runners_all.list(all=True)) и получает для каждого детальную информацию через gl.runners.get(id).
Для каждого раннера извлекаются:

id, runner_type (shared, group_type, project_type);

description, status, online, ip_address;

tag_list, contacted_at — метки и время последнего контакта.

Если раннер привязан к проекту или группе, из вложенных структур берутся:

source_name — имя проекта или группы;

source_path — путь (full_path или path_with_namespace).

Таким образом строится таблица Раннеры, которая показывает связь раннеров с командами, их тип и текущее состояние (активен / офлайн).

3. Формирование и структура отчёта

После сбора всех данных вызывается write_to_excel(), которая создаёт отчёт gitlab_report.xlsx с помощью xlsxwriter.
Форматирование таблиц унифицировано — заголовки выделяются серым фоном и рамками, содержимое выравнивается в сетку.
Файл включает четыре листа:

📄 Пользователи

Содержит список всех учётных записей GitLab.
Поля:

ID, Username, Email, Name;

Last Sign In, Last Activity;

Extern UID (внешние идентификаторы).
Позволяет определить активность пользователей и наличие интеграций (LDAP, OIDC).

📊 Статистика

Общие числовые показатели GitLab, включая количество проектов, групп, merge request'ов, пользователей, а также добавленные метрики:

total_commits — суммарное число коммитов;

projects_processed — количество обработанных проектов;

runners_total — общее число раннеров.
Используется для оценки нагрузки и состояния системы в целом.

📁 Проекты

Подробная таблица по каждому проекту.
Поля:

ID, Project Name, Namespace Path;

Repository Size (MB), LFS Size (MB), Artifacts Size (MB), Total Storage (MB);

Commits, Last Activity, Visibility.
Позволяет оценить объёмы данных и активность репозиториев.

⚙️ Раннеры

Информация обо всех зарегистрированных CI/CD-раннерах.
Поля:

ID, Source Name, Source Path, Runner Type, Description;

Status, Online, IP Address, Tag List, Contacted At.
Даёт возможность отследить состояние инфраструктуры сборок, привязку раннеров и их доступность.

4. Сохранение результатов и логирование

После записи всех листов файл сохраняется как gitlab_report.xlsx в рабочем каталоге.
Логирование ведётся в консоль и файл gitlab_report.log — фиксируются все ключевые шаги, включая подключение, получение данных, количество обработанных объектов и итог выполнения.

Финальное сообщение: