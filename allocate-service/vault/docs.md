1. Принцип отбора и взаимодействие с Vault API

После загрузки конфигурации из переменных окружения (.env) и аутентификации клиентом hvac к Vault выполняется обращение к нескольким внутренним API Vault.

1.1. Vault API-запросы

Для взаимодействия используется вспомогательная функция vault_request(method, path), которая поддерживает методы GET и LIST.
Через неё происходят следующие вызовы:

LIST identity/alias/id — получение всех алиасов аутентификации.
Возвращает словарь key_info, содержащий сведения о каждом алиасе: имя, тип аутентификации (mount_type), метаданные (например, service_account_name, namespace).
Эти данные используются для построения таблиц:

Aliases — детальный список всех алиасов;

Auth Types Summary — сводка по типам аутентификации (kubernetes, ldap, oidc и т. д.);

Unique Users — уникальные пользователи с учётом разных способов входа.

LIST auth/token/accessors — получение списка активных токенов.
Используется для подсчёта общего количества действующих токенов, информация попадает в лист Token Stats и Summary.

LIST auth/ldap/groups — получение списка LDAP-групп, настроенных для аутентификации Vault.
Эти имена затем используются при подключении к AD для сопоставления групп Vault с реальными объектами каталога.

2. Получение и парсинг метрик Vault

Для оценки состояния KV-хранилищ выполняется запрос:

GET /v1/sys/metrics?format=prometheus


или, при необходимости, к незащищённому эндпоинту /metrics.

Результат — Prometheus-совместимый текст, содержащий метрики по секретам.
Из него регулярным выражением извлекаются строки вида:

vault_secret_kv_count{mount_point="kv-team1"} 354


Далее выполняется парсинг:

mount_point — имя KV-монта (например, kv-team1);

count — количество секретов под этим монтированием.

Собранные данные формируют таблицу KV Mounts, а общая сумма (kv_total) используется в листе Summary.
Отдельно выделяются командные KV (kv-[team]-[code]), формирующие лист Team KV.

3. Определение алиасов и уникальных пользователей

Функция get_aliases() анализирует ответ от Vault /identity/alias/id и формирует список записей, каждая из которых описывает способ аутентификации и связанные метаданные.
Для каждого алиаса извлекаются:

name — имя в Vault;

mount_type — тип аутентификации (ldap, kubernetes, oidc и др.);

effective_username — реальное имя пользователя или service account;

namespace — пространство имён Vault, если используется.

Результат фильтруется и агрегируется:

Подсчёт количества алиасов по каждому типу аутентификации — таблица Auth Types Summary.

Полный перечень алиасов — таблица Aliases.

Функция get_unique_users() нормализует имена пользователей, убирая символы (., -, @домен), и формирует список уникальных пользователей по полю effective_username.
В результате строится таблица Unique Users, где для каждого пользователя указаны все связанные логины (all_logins) и namespaces, если они присутствуют.

4. Интеграция с Active Directory

После завершения сбора данных из Vault выполняется подключение к AD через LDAP3:

создаётся защищённое соединение ldaps:// с TLS-сертификатом (CA_CERT),

производится авторизация под учётной записью (AD_USER, AD_PASSWORD).

Далее для каждой группы, возвращённой из Vault (auth/ldap/groups), выполняется поиск в AD:

Поиск объекта группы в базе GROUP_SEARCH_BASE по фильтру (objectClass=group).

После получения DN группы строится LDAP-фильтр для поиска участников:

(memberOf=<groupDN>) — прямая принадлежность;

(memberOf:1.2.840.113556.1.4.1941:=<groupDN>) — рекурсивный поиск (вложенные группы), если INCLUDE_NESTED=True.

Для каждой найденной учётной записи запрашиваются атрибуты:

sAMAccountName,

displayName,

mail,

whenCreated,

distinguishedName.

Каждый пользователь добавляется в результирующий список ad_full_list, который в дальнейшем попадает в таблицу AD Group Members.

Таким образом, AD-запросы позволяют связать настройки Vault с фактическими группами безопасности и их участниками.

5. Формирование и структура отчёта

После сбора всех данных вызывается функция write_excel(), которая создаёт файл vault_usage_report.xlsx.
Отчёт формируется с использованием библиотеки xlsxwriter и содержит несколько тематических листов:

📘 Aliases

Подробный перечень всех алиасов Vault:

name — имя аутентификации,

mount_type — способ входа (ldap, kubernetes и др.),

namespace — пространство имён (если есть).

📊 Auth Types Summary

Агрегированная статистика по типам аутентификации:

auth_type — тип метода входа,

count — количество алиасов данного типа.

👤 Unique Users

Список уникальных пользователей, объединяющий разные способы входа:

unique_user — нормализованное имя пользователя,

all_logins — все логины и типы аутентификации, связанные с ним,

namespaces — перечень namespaces Vault, если они задействованы.

🔑 Token Stats

Общее количество активных токенов:

active_tokens — текущее число действующих accessors.

📦 KV Mounts

Информация о монтированиях KV и количестве секретов:

mount_point — имя KV-хранилища,

count — число секретов.

👥 AD Group Members

Результаты поиска пользователей в Active Directory:

ad_group — имя группы,

user — учётная запись,

displayName, mail — ФИО и e-mail,

user_dn, user_created — DN и дата создания учётки.

🧩 Team KV

Выделенные командные KV-хранилища, определяемые по шаблону kv-team-code.

📄 Summary

Сводная статистика по всей системе:

адрес Vault (Vault Address),

общее количество алиасов, пользователей, токенов,

количество KV-монтирований и секретов,

количество командных KV и найденных пользователей AD.

6. Завершение работы

После формирования всех листов Excel-файл автоматически форматируется — ширина столбцов подгоняется под содержимое.
Файл сохраняется в текущем каталоге под именем vault_usage_report.xlsx.
Логирование (в файл vault_ad_sync.log и консоль) отображает все этапы выполнения, включая количество обработанных групп, пользователей и длительность операций.