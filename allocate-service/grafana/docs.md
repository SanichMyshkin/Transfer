Скрипт выполняет комплексный аудит инфраструктуры Grafana, собирая данные через Grafana API и конфигурационные файлы из GitLab.
В результате формируется детализированный Excel-отчёт, объединяющий сведения об организациях, пользователях, папках, дашбордах и правах доступа на основе LDAP-групп.

Архитектура скрипта модульная:

gitlab_config_loader.py — загрузчик конфигураций из GitLab для анализа LDAP-групп и прав доступа;
main.py — основной управляющий модуль, объединяющий логику сбора данных из Grafana API, анализа организаций и построения отчёта.

1. Загрузка конфигураций из GitLab

1.1. GitLabConfigLoader

Инициализация: параметры подключения загружаются из переменных окружения:

GITLAB_URL, GITLAB_TOKEN — обязательные параметры аутентификации
GITLAB_PROJECT_ID — ID проекта (по умолчанию: "3058")
GITLAB_FILE_PATH — путь к файлу конфигурации (по умолчанию: "grafana_main/ldap.toml")
GITLAB_REF — ветка GitLab (по умолчанию: "main")
Подключение: используется библиотека python-gitlab с отключенной SSL-верификацией.

1.2. Загрузка и парсинг конфигураций

Метод load_raw_text():

Получает файл через GitLab Files API
Декодирует base64-содержимое в UTF-8 строку
Метод load_group_mappings():

Парсит TOML-конфигурацию с помощью tomllib/tomli
Извлекает секцию servers → group_mappings
Обрабатывает как словарные, так и списковые структуры конфигурации
Форматирует данные в структурированный вид:

org_id — идентификатор организации (обязательное поле)
group_dn — Distinguished Name LDAP-группы
org_role — роль в организации (Admin/Viewer/Editor)
grafana_admin — флаг глобальных прав администратора
1.3. Анализ прав доступа

Метод detect_org_owners():

Группирует маппинги по org_id
Определяет владельцев организаций по приоритету:

Internal Admins — группы с org_role: "Admin" и grafana_admin: false
Global Admins — группы с org_role: "Admin" и grafana_admin: true
Метод get_owners_clean():

Извлекает CN (Common Name) из DN LDAP-групп
Убирает дубликаты, сохраняя уникальные группы для каждой организации
Возвращает отсортированный список групп-владельцев
2. Сбор данных из Grafana API

2.1. Инициализация и аутентификация

Настройки из переменных окружения:

GRAFANA_URL, GRAFANA_USER, GRAFANA_PASS — данные для аутентификации
ORG_LIMIT — ограничение количества организаций для анализа (по умолчанию: 5)
SLEEP_AFTER_SWITCH, SLEEP_BETWEEN_CALLS — тайминги для избежания перегрузки API
Аутентификация: метод login_cookie() выполняет вход через /login эндпоинт и сохраняет сессионные куки.

2.2. Работа с организациями

Переключение организаций: switch_org(org_id) — POST запрос к /api/user/using/{org_id}
Получение названия: get_org_name(org_id) — GET запрос к /api/orgs/{org_id}
Пользователи организации: get_users_in_org(org_id) — GET запрос к /api/orgs/{org_id}/users

2.3. Сбор метрик дашбордов

Папки: get_folders() — получение всех папок организации с лимитом 5000
Дашборды в папках: get_dashboards_in_folder(fid) — поиск дашбордов по folderIds
Корневые дашборды: get_root_dashboards() — дашборды без папки (folderId = 0)
Панели дашбордов: get_dashboard_panels(uid) — глубокий анализ структуры дашборда:

Рекурсивный подсчёт панелей в секциях panels и rows
Учёт вложенных структур панелей
2.4. Глобальные пользователи Grafana

Метод get_all_grafana_users():

Постраничная загрузка через /api/users (лимит: 1000 на страницу)
Сбор полной информации о всех пользователях системы
3. Обработка и структурирование данных

3.1. Пользователи организаций

Собирается для каждой организации после успешного переключения контекста:

organization — название организации
user_id, email, login — идентификационные данные
role — роль пользователя в организации
3.2. Папки и дашборды

Папки:

folder_id, folder_title — идентификатор и название
dashboards_count — количество дашбордов в папке
Дашборды:

dashboard_uid, dashboard_title — идентификатор и название
panels — количество панелей в дашборде
Привязка к папке или корневой секции (ROOT)
3.3. Сводка по организациям

Для каждой организации формируется итоговая строка:

organization — название оргганизации
owner_group — группы-владельцы из LDAP
users_total — общее количество пользователей
folders_total — количество папок
dashboards_total — общее число дашбордов
panels_total — суммарное количество панелей
3.4. Глобальная статистика

Метрики системы:

organizations_total — всего организаций
users_total — всего пользователей Grafana
folders_total — всего папок
dashboards_total — всего дашбордов
panels_total — всего панелей во всех дашбордах
4. Формирование и структура отчёта

Данные записываются в файл grafana_report.xlsx с помощью библиотеки openpyxl.

📊 Summary — глобальная сводка по всей инфраструктуре Grafana

👥 GrafanaUsers — все пользователи системы с полными данными:

id, name, login, email — идентификационные данные
isAdmin, isDisabled — флаги прав и статуса
created, updated — временные метки
🏢 Organizations — сводка по организациям:

Название организации и группы-владельцы
Количественные метрики: пользователи, папки, дашборды, панели
Отметка "NO ACCESS" для организаций без прав доступа
👤 OrganizationUsers — детализированные данные пользователей по организациям:

Привязка к организации и роли
Email и логины пользователей
📁 Folders — инвентаризация папок:

Название папки и идентификатор
Количество дашбордов в каждой папке
📈 Dashboards — полный перечень дашбордов:

Название и UID дашборда
Принадлежность к папке
Количество панелей для анализа сложности
5. Логирование и обработка ошибок

Настройка логирования:

Двухканальное логирование (файл + консоль)
Формат: дата | уровень | сообщение
Кодировка UTF-8 для корректного отображения русских символов
Обработка ошибок доступа:

Организации без прав доступа отмечаются "NO ACCESS"
Продолжение обработки следующих организаций при ошибках
Визуальный прогресс-бар через tqdm для отслеживания выполнения
Оптимизация производительности:

Паузы между API-запросами для избежания rate limiting
Пакетная обработка данных
Лимитирование количества организаций для анализа
Итог:
Скрипт автоматизирует комплексный аудит инфраструктуры Grafana, объединяя данные из конфигураций LDAP (через GitLab) и метрик из Grafana API. Он предоставляет детализированный отчёт для анализа прав доступа, использования организаций, структуры дашбордов и распределения пользователей, что позволяет проводить эффективный мониторинг и оптимизацию системы визуализации.